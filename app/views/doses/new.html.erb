<h1>Add a new dose and ingredient</h1>

<%= simple_form_for [@cocktail, @dose] do |f| %>
    <%= f.input :description %>
    <%= f.association :ingredient%>
    <%= f.submit "submit the recipe", class: "btn btn-primary" %>
<% end %>


